"""
Debug package.

Tools for interactive, frame-by-frame inspection of CARLA scenes and sensor
configurations:

- :mod:`~debug.annotation_debug_tool`  — live annotation capture with pause /
  single-step / capture controls and visual bounding-box overlays.
- :mod:`~debug.interactive_sensor_tool` — real-time sensor positioning with
  FOV-triangle visualisation and JSON save/load.
"""
